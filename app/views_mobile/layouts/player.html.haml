!!!
%html{lang: "en"}
  %head
    %title= title
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}/
    %link{ :rel => "shortcut icon", :href => "/assets/fallback/thumb_favicon_default.jpg", :type => "image/x-icon"}
    = render "layouts/meta"
    = csrf_meta_tags
    = javascript_include_tag "mobile"
    = stylesheet_link_tag    "mobile"
    = render "layouts/analytics"

    :javascript
      window.addEventListener("load",function() {
        // Set a timeout...
        setTimeout(function(){
          // Hide the address bar!
          window.scrollTo(0, 1);
        }, 0);
      });

  %body#stage{:class => body_id}
    = render "layouts/facebook"
    %header#header
      = link_to new_invites_path do
        = image_tag "moziy-logo-2.png", "data-at2x" => image_path("moziy-logo-2@2x.png")
      %span.tagline where videos come to play
    = yield
    %footer#footer
      shared via moziy
      = link_to "sign up", new_invites_path
