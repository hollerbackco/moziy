_/<%= "#{@comment.user.username}" %>_ commented on *<%= @airing.title %>*:

    "<%= @comment.body %>"

Reply at <%= edit_manage_channel_video_url(@airing.channel, @comment.commentable)%>.

moziy.com
