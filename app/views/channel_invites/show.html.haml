#channel-invite
  .invite-copy
    = @user.username
    would like you to collaborate on this channel.
  #channel-card.clearfix
    = image_tag @channel.cover_art_url(:thumb_player), class: "player"
    %h2.title= "#{@channel.title}"
    .slug= "/#{@channel.slug}"
    .description= @channel.description
    .followers= pluralize @channel.subscriptions.count, "follower"
  %p
    = link_to "Accept Invite", accept_join_channel_path(@token), class: "button primary"
