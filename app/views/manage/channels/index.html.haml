#channel-list
  %ul.channels
    - index = 0
    - @channels.each do |c|
      - if c.has_airings?
        %li.channel
          %span.count= "+#{c.subscriptions.count}"
          = link_to "#{c.title}", manage_channel_videos_path(c), :class => "color#{index % 3}"
        - index += 1
#viewer
